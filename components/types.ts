export type ArticleData = {
  title: string;
  description: string;
  date: string;
  category: string;
  wordCount: number;
  readingTime: string;
};

export enum ArticleType {
  TECHNOLOGY = 'Technology',

}
