/** @type {import('next-sitemap').IConfig} */
module.exports = {
  // Canonical base URL of your site
  siteUrl: 'https://philp.io',

  // You already have a custom robots.txt in public/, so don't overwrite it
  generateRobotsTxt: false,

  // Optional: reasonable defaults for a personal blog
  changefreq: 'weekly',
  priority: 0.7,
};


